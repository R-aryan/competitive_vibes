#include<iostream>
#include<cmath>
using namespace std;
int main()
{
    int t;
    cin>>t;
    int r[t],w[t];
    long long i,j,k,l;
    for(i=0;i<t;i++)
    {
        cin>>r[i]>>w[i];
        }

        for(i=(-pow(10,9));i<=pow(10,9);i++)
        {
            for(j=(-pow(10,9));j<=pow(10,9);j++)
            {


                for(k=(-pow(10,9));k<=pow(10,9);k++)
                {


                for(l=(-pow(10,9));l<=pow(10,9);l++)
                {

                }
            }
            }
        }
cout<<"Hello World";

return 0;
}
